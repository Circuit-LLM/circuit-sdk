{
  "name": "@circuit/agent",
  "version": "0.2.1",
  "description": "Circuit SDK agent runtime — write an autonomous agent (CircuitAgent) with pluggable custody (paper · self-custody · off-box signer · non-custodial vault). Buy/sell only; on the mesh the key stays off-box, so funds can't leave.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --experimental-strip-types --conditions=development --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsup src/index.ts --format esm --dts --clean --out-dir dist && tsup bin/circuit-agent.ts --format esm --out-dir dist",
    "prepack": "tsup src/index.ts --format esm --dts --clean --out-dir dist && tsup bin/circuit-agent.ts --format esm --out-dir dist"
  },
  "dependencies": {
    "@circuit/inference": "0.2.1",
    "@circuit/data": "0.2.1",
    "@circuit/attest": "0.2.1"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "bin": {
    "circuit-agent": "dist/circuit-agent.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Circuit-LLM/circuit-sdk.git",
    "directory": "packages/agent"
  },
  "homepage": "https://github.com/Circuit-LLM/circuit-sdk/tree/main/packages/agent#readme",
  "bugs": {
    "url": "https://github.com/Circuit-LLM/circuit-sdk/issues"
  },
  "engines": {
    "node": ">=18"
  }
}
