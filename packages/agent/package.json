{
  "name": "@circuit/agent",
  "version": "0.0.0",
  "description": "Circuit SDK agent runtime — write an autonomous agent (CircuitAgent) that runs on Circuit's CPU mesh with off-box (non-custodial) signing. Buy/sell only; funds can't leave.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@circuit/core": "0.0.0",
    "@circuit/inference": "0.0.0",
    "@circuit/data": "0.0.0"
  }
}
