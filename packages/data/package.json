{
  "name": "@circuit/data",
  "version": "0.0.0",
  "description": "Circuit SDK data — typed client for the Circuit Data API (market + on-chain data), paid per call in CIRC (x402).",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@circuit/core": "0.0.0",
    "@circuit/x402": "0.0.0"
  }
}
