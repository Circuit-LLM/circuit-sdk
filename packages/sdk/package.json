{
  "name": "@circuit/sdk",
  "version": "0.0.0",
  "description": "Circuit SDK — batteries-included meta-package re-exporting @circuit/{core,x402,inference,data,wallet}.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --experimental-strip-types --conditions=development --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsup src/index.ts --format esm --dts --clean --out-dir dist",
    "prepack": "tsup src/index.ts --format esm --dts --clean --out-dir dist"
  },
  "dependencies": {
    "@circuit/core": "0.0.0",
    "@circuit/x402": "0.0.0",
    "@circuit/inference": "0.0.0",
    "@circuit/data": "0.0.0",
    "@circuit/wallet": "0.0.0",
    "@circuit/agent": "0.0.0",
    "@circuit/node": "0.0.0",
    "@circuit/onchain": "0.0.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ]
}
