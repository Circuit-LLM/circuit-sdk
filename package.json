{
  "name": "circuit-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Circuit SDK — TypeScript toolkit for the Circuit decentralized-LLM ecosystem (x402-paid inference, data, wallet, and hosted agents with off-box custody). See SDK.md.",
  "license": "UNLICENSED",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test packages/*/test/*.test.ts",
    "typecheck": "for p in core x402 inference data wallet agent node onchain sdk; do tsc -p packages/$p/tsconfig.json || exit 1; done"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
