{
  "name": "circuit-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Circuit SDK — TypeScript toolkit for the Circuit decentralized-LLM ecosystem (x402-paid inference, data, wallet, and hosted agents with off-box custody). See SDK.md.",
  "license": "UNLICENSED",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "cli": "node apps/cli/bin/circuit.js",
    "test": "node --experimental-strip-types --conditions=development --test packages/*/test/*.test.ts",
    "typecheck": "for p in core x402 attest inference data wallet agent node onchain bundle vault sdk; do tsc -p packages/$p/tsconfig.json || exit 1; done",
    "build": "for p in core x402 attest inference data wallet agent node onchain bundle vault sdk; do (cd packages/$p && npx tsup src/index.ts --format esm --dts --clean --out-dir dist) || exit 1; done"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.5.1",
    "typescript": "^5.7.2"
  }
}
